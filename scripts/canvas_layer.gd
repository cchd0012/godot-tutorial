extends CanvasLayer


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	var world_position = Vector2(100, 100)
	#var screen_position = camera_2d.world_to_screen(world_position)
	#score_label.position = world_position
